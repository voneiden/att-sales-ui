export enum ROUTES {
  AUTH_ERROR = 'auth-error',
  CUSTOMERS = 'customers',
  INDEX = '/',
  LOGIN = 'login',
  LOGOUT = 'logout',
  NOT_FOUND = '404',
  PROJECTS = 'projects',
}

export enum StateOfSale {
  ForSale = 'FOR_SALE',
  PreMarketing = 'PRE_MARKETING',
  Upcoming = 'UPCOMING',
}
