export enum ROUTES {
  AUTH_ERROR = 'auth-error',
  INDEX = '/',
  LOGIN = 'login',
  LOGOUT = 'logout',
  NOT_FOUND = '404',
  PROJECT = 'project',
}

export enum StateOfSale {
  ForSale = 'FOR_SALE',
  PreMarketing = 'PRE_MARKETING',
  Upcoming = 'UPCOMING',
}
